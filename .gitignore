# ── Generated by Ashen ────────────────────────────────────────────────────
# Rewritten by the Theme tab (and by matugen in dynamic mode) on every scheme
# change, so tracking it means a diff every time the colors change.
# kitty only warns if the include is missing; the file reappears the first time
# a scheme is applied.
kitty/.config/kitty/ashen-colors.conf

# matugen writes these from the wallpaper on every change. They belong in
# ~/.config, not here — this only catches the case where stow folded the
# gtk-3.0/gtk-4.0 directories into symlinks back at the repo.
gtk/.config/gtk-3.0/gtk.css
gtk/.config/gtk-4.0/gtk.css

# ── Editor / OS noise ─────────────────────────────────────────────────────
*.swp
*.swo
*~
*.bak
*.orig
*.rej
.DS_Store
Thumbs.db
.idea/
.vscode/

# ── Shell / runtime junk ──────────────────────────────────────────────────
*.log
.zcompdump*
__pycache__/
*.pyc

# Written by Settings > Input when you change a keybind: a table of id -> keys
# that keybinds.lua asks before it binds anything. ~/.config/hypr is a symlink
# into this checkout, so the generated file lands here; tracking it would make
# the tree dirty and the installer skip its own self-update.
hypr/.config/hypr/conf/userkeys.lua

# ── Probe renders ─────────────────────────────────────────────────────────
# run-mark-probe.sh DRAWS mark.png with qml6 and then measures it, so tracking
# it is storing a probe's own output. The sddm shots are what a verification
# session looked like on a screen, not fixtures -- nothing reads them back, and
# they were two thirds of the repository.
docs/probes/mark.png
docs/probes/sddm/*.png
