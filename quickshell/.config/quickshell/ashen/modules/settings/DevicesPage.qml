import QtQuick
import QtQuick.Layouts
import "root:/modules/settings/components"

// What the machine plays through and records with. The keyboard and the apps
// it opens are next door in Input; the radios are in Network.
TabPage {
    SettingsSoundTab {}
}
