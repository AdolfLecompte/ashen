import QtQuick
import "root:/modules/settings/components"

// Everything you drive the machine WITH: what the keys are laid out as, which
// programs the four app keys open, and which keys those are.
TabPage {
    SettingsInputTab {}
}
